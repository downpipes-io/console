{
  "$schema": "./node_modules/@stryker-mutator/core/schema/stryker-schema.json",
  "_comment": "StrykerJS mutation-testing config for the console's in-browser crypto surface (the key ceremony: Shamir secret sharing, the recipient envelope, and the WebAuthn PRF key path). GATING via the scheduled .github/workflows/mutation.yml: a run below thresholds.break fails. Project standard: break 90, high 95. The command runner re-uses the existing crypto validators as the oracle, so no extra test wiring is needed. Reaching the score is the tracked test-writing campaign. Run with: npm run test:mutation",
  "packageManager": "npm",
  "ignorePatterns": ["coverage", ".stryker-tmp", "dist", "public"],
  "testRunner": "command",
  "commandRunner": {
    "command": "node test/validate-bytes.ts && node test/validate-shamir.ts && node test/validate-envelope.ts && node test/validate-webauthn-prf.ts"
  },
  "mutate": [
    "src/lib/shamir.ts",
    "src/lib/envelope.ts",
    "src/lib/webauthn-prf.ts"
  ],
  "reporters": ["clear-text", "progress", "html"],
  "htmlReporter": { "fileName": "coverage/mutation/index.html" },
  "thresholds": { "high": 95, "low": 90, "break": 90 },
  "timeoutMS": 60000,
  "concurrency": 2,
  "tempDirName": ".stryker-tmp"
}
